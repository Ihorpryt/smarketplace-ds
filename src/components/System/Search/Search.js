import React from "react";
import Select, { components } from "react-select";

const Search = () => {
    return (
        <Select
            placeholder={"Search"}
        />
    );
};

export default Search;