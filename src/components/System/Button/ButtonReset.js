import React from "react";
import {SButtonReset} from "./styled";

const ButtonReset = ({text}) => {
    return (
        <SButtonReset>
            {text}
        </SButtonReset>
    );
};

export default ButtonReset;